[tox]
envlist = pypy, py26, py27, py33, py34

[testenv]
usedevelop = True
deps =
    crate[test] >= 0.13.2
    prompt-toolkit
    zope.testrunner
    py26: argparse
    colorama
commands =
    zope-testrunner -c --test-path=src
